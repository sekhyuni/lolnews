name: Nodejs_build

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

#   workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
#       - uses: actions/checkout@v3
      - name: Run pwd
        run: pwd
      - name: Run ls -al
        run: ls -al
